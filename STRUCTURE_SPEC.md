# Flashboard 構造仕様書

最終更新: 2026-03-05  
対象: `flashboard-app`（`README.md` 同階層）

## 1. 目的
- UI主導で進んだ現実装を、運用可能なプロダクト実装へ収束させるための基準仕様を定義する。
- 「何を作るか」ではなく「どの構造で、どの完了条件を満たせば完成か」を明文化する。
- 今後の実装・レビュー・タスク分解・外注依頼の共通参照点とする。

## 2. 現状サマリ（2026-03-05時点）
- 強み:
1. 記事収集→Gemini要約→Firestore保存の基幹フローは存在する。
2. UIはダッシュボードとして成立しており、ニュース閲覧体験は実装済み。
3. Cron API（`/api/cron/sync`）とローカル同期テスト（`npm run test:sync`）がある。
- 課題:
1. 管理者判定がハードコードされている（`src/app/page.tsx`）。
2. Firestoreルールで `articles` への書き込みが「ログインユーザー全員」に開放されている。
3. クライアントから直接 `syncRss` を呼ぶ構成で、権限境界が曖昧。
4. エラー観測・再実行設計・重複防止・性能制御の要件が未定義。
5. 機能の実装有無と完成基準（DoD）がREADMEだけでは不十分。

## 3. 完成定義（プロダクトDoD）
- 下記が満たされた時点を「完成」とする。
1. 同期処理が1日運用で安定稼働し、致命的失敗時に検知・再実行できる。
2. `articles` 書き込み経路がサーバー経由のみに制限され、不正更新できない。
3. 管理機能（手動同期）は `ADMIN_EMAIL` など環境変数ベースで統一管理される。
4. FACE出力（Flash/Act/Context/Effect/Tags）が欠損時にもUIが破綻しない。
5. 最低限の検証（型チェック + 同期フロー試験 + 権限制御確認）がCIまたは手順として固定化される。

## 4. 目標アーキテクチャ
- フロント:
1. Next.js App Routerで閲覧UI・認証UI・管理操作UIを提供。
2. Firestoreの読み取りはクライアント可、更新は用途ごとに制限。
- バックエンド:
1. `GET /api/cron/sync` を同期エントリポイントとする。
2. 同期実体は `src/ai/flows/sync-rss-flow.ts` に集約し、APIとCLIの双方から呼ぶ。
3. Firestore書き込みはAdmin SDK経由に統一（クライアントの直接書き込みを最小化）。
- AI:
1. GeminiによるFACE生成は `summarize-aggregated-article-content-flow.ts` で一元化。
2. 失敗時はフォールバック保存し、後続再処理可能なデータ形に保つ。

## 5. ディレクトリ責務
- `src/app/`:
1. 画面ルーティング、ページ構成、API Route。
- `src/components/`:
1. UIコンポーネント、認証/ダッシュボード表示部品。
- `src/ai/flows/`:
1. 同期・要約など業務フローの中核ロジック。
- `src/firebase/`:
1. Firebase初期化、プロバイダ、Admin SDK連携。
- `scripts/`:
1. ローカル運用スクリプト（同期試験など）。
- `docs/`:
1. 企画・補助資料。実装基準は本仕様書を正とする。

## 6. データ仕様（Firestore）
- `articles/{articleId}`:
1. 必須: `title`, `link`, `sourceName`, `publishedAt`, `category`
2. 推奨: `translatedTitle`, `act`, `context`, `effect`, `tags`, `imageUrl`
3. 監査: `createdAt`, `updatedAt`
- `users/{userId}/sources/{sourceId}`:
1. `name`, `url`, `category`, `createdAt`
- `users/{userId}/bookmarks/{bookmarkId}`:
1. `articleId`, `title`, `url`, `tags`, `bookmarkedAt`

## 7. 権限仕様（必須）
- 管理者判定:
1. `ADMIN_EMAIL` を正とし、コードハードコードを禁止する。
- 手動同期:
1. UI表示・実行可否ともに管理者判定を通す。
2. 実行はAPI経由に統一し、クライアントからフロー関数を直接実行しない。
- Firestore Rules:
1. `articles` は `read: true`、`write: false`（またはサーバー専用経路）。
2. `users/{userId}` 以下は `isOwner(userId)` を維持。

## 8. 同期フロー仕様
- 入力:
1. ソース配列（`name`, `url`, `category`）
- 処理:
1. RSS取得（1ソース最大10件）
2. 既存記事判定（`link`一致）
3. 要約未生成記事のみGemini実行
4. 成功時: FACE項目付きで保存
5. 失敗時: フォールバックで保存し `aiErrors` に記録
- 出力:
1. `addedCount`, `updatedCount`, `processedSources`, `errors`, `aiErrors`

## 9. 非機能要件
- 信頼性:
1. 同期失敗はレスポンスとログに必ず残る。
2. 同期処理は途中失敗しても他ソース処理を継続する。
- 性能:
1. APIタイムアウト内で完了する件数に制限する（現状1ソース10件）。
2. 必要時はソース並列ではなく段階的最適化を行う（まず安定性優先）。
- 運用:
1. Cron secret未設定時は明示的に失敗させる。
2. ローカル検証手順（`npm run test:sync`）を維持する。

## 10. 優先実装バックログ（P0/P1/P2）
- P0（最優先・先にやる）:
1. 管理者メールのハードコード撤去（環境変数統一）。
2. `articles` 書き込み権限を閉じる（Rules改修）。
3. 手動同期の呼び出しをAPI経由へ変更。
4. 同期APIの実行ログを最低限整備（開始/終了/件数/失敗）。
- P1（安定化）:
1. 要約失敗記事の再処理方針を定義（再試行条件）。
2. カスタムRSS追加時のURLバリデーション・重複防止。
3. ブックマーク重複登録防止。
- P2（拡張）:
1. 管理画面で同期履歴を可視化。
2. フィルタ・検索の永続化。
3. 通知/メルマガ等の派生機能。

## 11. 各項目の完了条件（実装タスクDoD）
- セキュリティ:
1. 一般ユーザーが `articles` へ直接書き込みできないことを確認。
- 手動同期:
1. 管理者ログイン時のみボタン表示、非管理者は非表示/拒否されること。
2. ボタン押下でAPI経由同期し、結果トーストが表示されること。
- 同期品質:
1. 1回の実行で `addedCount + updatedCount` が0以上で返ること。
2. RSS失敗時もAPIが500で落ち切らず、エラー配列に集約されること。
- ドキュメント:
1. READMEと本仕様書で矛盾がないこと。

## 12. 運用ルール
- 仕様変更:
1. 仕様を変えるPRは、本ファイルの該当節を同時更新する。
- 実装判断:
1. UI都合と構造仕様が衝突した場合、本仕様書を優先する。
- 記録:
1. 重要な設計判断は「日付 + 理由 + 影響範囲」を追記する。

## 13. 直近マイルストーン
- M1（安全化）:
1. P0完了 + 同期が本番で安全に回る状態。
- M2（安定化）:
1. P1完了 + 運用時の手戻り低減。
- M3（拡張準備）:
1. P2着手可能な基盤状態。
